{
  "name": "proxy-server-nodejs",
  "version": "1.0.0",
  "description": "A simple open-source proxy server to avoid CORS error, written in Node.js",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors-anywhere": "^0.4.4"
  }
}
